*.tif
*.dem
*.tiff
*.stl
*.vrt
*.png
*.aux.xml
.DS_Store
country_dems/
__pycache__/
__eqearth_intermediates/

# QC harness run outputs (timestamped JSON reports)
qc_check_*.json
qc_pair_*.json
qc_baseline_*.json

# Docker compose placeholder dirs (created when env vars unset)
_no_dem_data_set/
outputs/
.env

# Output directories
STLs_*/

# Data directories (large files, shouldn't be in repo)
data/ne/*.shp
data/ne/*.shx
data/ne/*.dbf
data/ne/*.prj
data/ne/*.cpg
data/ne/*.zip
data/ne/*.html
data/ne/*.txt

# Tile lists / s5cmd batches (generated, can be recreated)
*_tile_urls_s3.txt
tileList.txt
*_s5cmd*.txt

# Tile directories (large DEM files)
caucasus_tiles/
asia_tiles/
STLs/
